{
  "title": "asd",
  "description": "asd is a fast, offline-friendly Android SDK component downloader for Linux and macOS that fetches packages from the Myket mirror, bypassing sdkmanager and Google's servers.",
  "0.0.0": {
    "Getting Started": {
      "Getting Started": "`asd` is a single Bash script plus a `setup` installer. It needs a handful of\nstandard command-line tools already on most Linux systems.\n\n## 1. Install dependencies\n\n| Dependency | Purpose |\n| --- | --- |\n| `aria2c` | Multi-threaded HTTP download |\n| `unzip` | Archive extraction |\n| `awk` | CSV parsing |\n| `curl` | Registry updates |\n| `sha1sum` | Checksum verification |\n\n**Arch Linux:**\n\n```bash\nsudo pacman -S aria2 unzip gawk curl coreutils\n```\n\n**Debian / Ubuntu:**\n\n```bash\nsudo apt install aria2 unzip gawk curl coreutils\n```\n\n**macOS (Homebrew):**\n\n```bash\nbrew install aria2 unzip gawk curl coreutils\n```\n\n## 2. Get the source and run setup\n\n```bash\ngit clone https://github.com/psdkjoon/asd.git\ncd asd\nsudo ./setup\n```\n\nThis installs the `asd` binary to `/usr/local/bin` and the CSV registry\ndirectory to `/opt`. The `setup` script auto-detects whether `sudo` is\nactually needed for the target directories, so if you point it at\nuser-owned paths (see below) it will skip privilege escalation entirely.\n\nPrefer not to touch system directories? Install to your user profile\ninstead — no password prompt required:\n\n```\n./setup ~/.local/bin ~/.local/share/asd\nexport PATH=\"$HOME/.local/bin:$PATH\"\nexport ASD_CSV=~/.local/share/asd/myket.csv\n```\n\nAdd the `export` lines to your `~/.bashrc` or `~/.zshrc` to make them\npermanent. See [Configuration](/asd/configuration/) for every environment\nvariable `asd` understands, and\n[Bypassing sudo](/asd/guides/bypassing-sudo/) for the full no-root setup.\n\n## 3. Download the package registry\n\n`asd` resolves package names and versions from a local CSV file rather than\nquerying the network on every lookup. Fetch it once before installing\nanything:\n\n```bash\nasd -up\n```\n\nThis downloads `sdk-archives.csv` from `maven.myket.ir`, filters it down to\nLinux-only entries, and stores it at `$ASD_CSV` (`/opt/myket.csv` by\ndefault).\n\n## 4. Install your first component\n\n```bash\nasd platform-tools\n```\n\n`asd` looks up `platform-tools` in the registry, downloads it with `aria2c`,\nverifies its SHA-1 checksum, extracts it under `$ANDROID_HOME`\n(`~/Android/Sdk` by default), and writes a `package.xml` so the Android SDK\ntooling recognizes it as installed.\n\nCheck that it worked:\n\n```bash\nasd --installed\n```\n\n## Next steps\n\n- Read [Configuration](/asd/configuration/) to point `asd` at a custom SDK\n  location or registry path.\n- Browse the [Guides](/asd/guides/) for common workflows like installing the\n  NDK, repairing broken packages, and verifying checksums.\n- Look up any command’s exact syntax in the\n  [Command Reference](/asd/command-reference/).",
      "Configuration": "`asd` is configured entirely through environment variables — there is no\nconfig file to edit.\n\n| Variable | Default | Description |\n| --- | --- | --- |\n| `ANDROID_HOME` | `~/Android/Sdk` | Android SDK root directory that components are installed into. |\n| `ASD_CSV` | `/opt/myket.csv` | Path to the local CSV package registry. |\n| `ASD_ARIA2C_OPTS` | `-x 16 -s 16 -k 1M` | Extra options passed straight through to `aria2c` for every download. |\n\n## Custom SDK location\n\n```bash\nexport ANDROID_HOME=/path/to/your/android-sdk\nexport ASD_CSV=/path/to/myket.csv\nasd ndk\n```\n\n`asd` creates `$ANDROID_HOME/<package-path>` for each component it installs,\nmirroring the same directory layout `sdkmanager` uses — so existing tooling\n(Android Studio, Gradle, `sdkmanager` itself) will recognize components\ninstalled by `asd` without any extra setup.\n\n## Custom registry path\n\nSetting `ASD_CSV` to a location outside `/opt` is also how you avoid `sudo`\nfor registry updates — see\n[Bypassing sudo](/asd/guides/bypassing-sudo/) for the full explanation:\n\n```bash\nexport ASD_CSV=~/.local/share/asd/myket.csv\nmkdir -p ~/.local/share/asd\nasd -up\n```\n\n## Tuning download speed\n\n`ASD_ARIA2C_OPTS` is passed directly to `aria2c`, so any `aria2c` flag is\nvalid here. The default requests 16 connections split across 16 segments\nwith a minimum split size of 1 MiB:\n\n```bash\nexport ASD_ARIA2C_OPTS=\"-x 16 -s 16 -k 1M\"\n```\n\nTo throttle bandwidth instead:\n\n```bash\nexport ASD_ARIA2C_OPTS=\"-x 4 -s 4 --max-download-limit=2M\"\nasd ndk\n```\n\n## Making it permanent\n\nAdd whichever variables you use to your shell profile so they persist across\nsessions:\n\n```bash\necho 'export ANDROID_HOME=$HOME/Android/Sdk' >> ~/.bashrc\necho 'export ASD_CSV=$HOME/.local/share/asd/myket.csv' >> ~/.bashrc\n```"
    },
    "Guides": {
      "Guides": "Practical, task-focused walkthroughs. Start with\n[Installing Components](/asd/guides/installing-components/) if you’re just\nlooking up how to grab a specific NDK or build-tools version.\n\n[### Installing Components\n\nFinding, installing, and listing SDK components by name and version.](/asd/guides/installing-components/)\n[### Bypassing sudo\n\nRun asd entirely out of your home directory, no root required.](/asd/guides/bypassing-sudo/)\n[### Repairing & Verifying\n\nDetect and fix broken installs, and verify component checksums.](/asd/guides/repairing-verifying/)\n\nEvery command used in these guides is documented in full in the\n[Command Reference](/asd/command-reference/).",
      "Installing Components": "## Update the registry first\n\n`asd` resolves everything from a local CSV file. Before installing anything\nfor the first time (or if you suspect the registry is stale), refresh it:\n\n```bash\nasd --update\n```\n\n`-up` is the short form. This fetches `sdk-archives.csv` from\n`maven.myket.ir`, drops any Windows/macOS-only rows, and saves the result to\n`$ASD_CSV`.\n\n## Find a package name\n\nList everything available, optionally filtered by a substring match against\nthe package path or display name:\n\n```bash\nasd --list           # every package in the registry\nasd --list ndk        # anything matching \"ndk\"\nasd -l build-tools\n```\n\nEach row shows the package path (what you pass to `asd install`), its\ndisplay name, and version:\n\n```\nPACKAGE                                 NAME                                          VERSION\nndk;27.0.12077973                       NDK (Side by side)                            27.0.12077973\nbuild-tools;37.0.0-rc2                  Android SDK Build-Tools 37-rc2                37.0.0-rc2\n```\n\n## Install it\n\n```bash\nasd ndk                          # latest matching version\nasd ndk 27.0.12077973            # a specific version\nasd build-tools 37.0.0-rc2\nasd platform-tools\n```\n\n`asd` downloads the archive with `aria2c`, checks its SHA-1 against the\nregistry, extracts it under `$ANDROID_HOME`, flattens a redundant\nsingle-subdirectory wrapper if the archive has one, and writes a\n`package.xml` so the component is recognized by the Android SDK tooling.\n\n## Confirm what’s installed\n\n```bash\nasd --installed         # everything found under $ANDROID_HOME\nasd --installed ndk     # just the NDK entries\n```\n\nThis walks `$ANDROID_HOME` for `package.xml` files rather than reading the\nregistry, so it reflects what’s actually on disk.",
      "Bypassing sudo": "By default `asd` writes the CSV registry to `/opt`, and `setup` installs the\nbinary to `/usr/local/bin` — both of which typically require root. Neither\nis required: point everything at directories your user already owns.\n\n## Install asd itself without sudo\n\n```\n./setup ~/.local/bin ~/.local/share/asd\n```\n\n`setup` checks whether each target directory is writable before calling\n`sudo`, so pointing it at your home directory skips privilege escalation for\nthe binary and the (optional) bundled CSV entirely.\n\n## Avoid sudo on registry updates\n\n`asd --update` writes to `$ASD_CSV`, which defaults to `/opt/myket.csv`. Set\nit to a user-owned path instead:\n\n```bash\nexport ASD_CSV=~/.local/share/asd/myket.csv\nmkdir -p ~/.local/share/asd\nasd --update\n```\n\nWith `ASD_CSV` pointed at a writable location, `asd --update` (and the\nautomatic registry fetch that happens the first time you run `asd install`)\nnever calls `sudo`.\n\n## Make it permanent\n\n```bash\ncat >> ~/.bashrc <<'EOF'\nexport PATH=\"$HOME/.local/bin:$PATH\"\nexport ASD_CSV=\"$HOME/.local/share/asd/myket.csv\"\nEOF\n```\n\n## Full no-root setup, start to finish\n\n```bash\ngit clone https://github.com/psdkjoon/asd.git\ncd asd\n./setup ~/.local/bin ~/.local/share/asd\n\nexport PATH=\"$HOME/.local/bin:$PATH\"\nexport ASD_CSV=\"$HOME/.local/share/asd/myket.csv\"\n\nasd --update\nasd platform-tools\n```\n\nNo step here touches a directory outside your home, so no password prompt\nappears at any point.",
      "Repairing & Verifying": "## Detecting broken installs\n\n`asd --fix` scans every installed component (every `package.xml` under\n`$ANDROID_HOME`) and looks for directories with no content files besides the\n`package.xml` itself — a common symptom of an interrupted extraction.\n\nCheck everything:\n\n```bash\nasd --fix\n```\n\n```\nChecking all installed packages for issues ...\nFixing: NDK (Side by side) (27.0.12077973) ...\nFixed 1 package(s).\n```\n\nCheck (and fix) just one component:\n\n```bash\nasd --fix ndk\n```\n\nIf an issue is found, `asd` removes the broken directory and reinstalls the\nexact same version from the registry — the same code path as\n`asd install`, so the SHA-1 checksum is re-verified on the way back in.\n\n## Verifying a checksum\n\n`asd --fix` only checks whether files exist, not whether their content is\ncorrect. To confirm an already-installed component matches the registry’s\nSHA-1 exactly, re-download and hash it with `--verify`:\n\n```bash\nasd --verify ndk 27.0.12077973\n```\n\n```\nDownloading archive to verify checksum ...\nRegistry checksum:  3a7f1e2c9b8d4f6a1e5c7b9d3f2a8e6c4b1d9f7a\nDownloaded checksum: 3a7f1e2c9b8d4f6a1e5c7b9d3f2a8e6c4b1d9f7a\nChecksum OK. Archive is authentic.\n```\n\nThis re-downloads the archive to hash it, so it’s a heavier operation than\n`--fix` — reach for it when you specifically suspect tampering or corruption\nrather than as a routine check.\n\n## Removing a component entirely\n\nIf repairing isn’t what you want and you’d rather start clean:\n\n```bash\nasd --remove ndk\nasd ndk\n```\n\nSee the [Command Reference](/asd/command-reference/) for the exact\nsemantics of `--fix`, `--verify`, and `--remove`."
    },
    "Command Reference": {
      "Command Reference": "```yaml\nUsage: asd <command> [args]\n```\n\nEvery command `asd` supports, in the order they appear in `asd --help`.\n\n| Command | Description |\n| --- | --- |\n| [`asd <name> [version]`](/asd/commands/install/) | Download and install an SDK component |\n| [`asd --list, -l [name]`](/asd/commands/list/) | List available packages (optionally filter by name) |\n| [`asd --installed, -i [name]`](/asd/commands/installed/) | List installed SDK components (optionally filter by name) |\n| [`asd --remove, -rm <name>`](/asd/commands/remove/) | Remove an installed SDK component |\n| [`asd --fix, -f [name]`](/asd/commands/fix/) | Repair broken or missing packages (optionally fix by name) |\n| [`asd --update, -up`](/asd/commands/update/) | Update the package registry from remote |\n| [`asd --verify, -v <name>`](/asd/commands/verify/) | Verify checksum of an installed component |\n| `asd --help, -h` | Show the help message above |\n\nAny command that reads the registry (everything except `--installed` and\n`--help`) will auto-download it on first use if `$ASD_CSV` doesn’t exist yet\n— you don’t strictly have to run `--update` manually first, though doing so\nis recommended so the fetch doesn’t happen silently mid-install.",
      "Install": "*Part of the [Command Reference](/asd/command-reference/).*\n\n```bash\nasd <name> [version]\n```\n\nThe default action — running `asd` with a bare package name (no leading\n`-`) installs it. This is the only command without an explicit flag.\n\n## Arguments\n\n- **`name`** — the package path from the registry, e.g. `ndk`,\n  `build-tools`, `platform-tools`, or a `;`-qualified path like\n  `platforms;android-34`.\n- **`version`** *(optional)* — an exact version string, e.g.\n  `27.0.12077973`. If omitted, the first matching entry in the registry is\n  used.\n\n## Examples\n\n```bash\nasd ndk                          # latest matching NDK entry\nasd ndk 27.0.12077973            # a specific NDK version\nasd build-tools 37.0.0-rc2\nasd platform-tools\nasd \"platforms;android-34\"\n```\n\n## What it does\n\n1. Ensures `$ASD_CSV` exists, downloading the registry first if needed.\n2. Looks up `name` (and `version`, if given) in the registry.\n3. Downloads the archive with `aria2c`, using `$ASD_ARIA2C_OPTS`.\n4. Verifies the download’s SHA-1 against the checksum in the registry, if\n   one is present.\n5. Extracts the archive under `$ANDROID_HOME/<package-path>`.\n6. Flattens the result if the archive contained a single wrapping\n   subdirectory.\n7. Writes a `package.xml` describing the component, so the Android SDK\n   tooling (and `asd --installed`) recognizes it.\n\n## Errors\n\n```yaml\nError: Package 'foo' (Version: latest) not found.\n```\n\nThe name (and version, if given) didn’t match anything in the registry —\ndouble check with [`asd --list`](/asd/commands/list/).",
      "List (--list, -l)": "*Part of the [Command Reference](/asd/command-reference/).*\n\n```bash\nasd --list [name]\nasd -l [name]\n```\n\nLists packages available in the local registry — not what’s installed (see\n[`--installed`](/asd/commands/installed/) for that).\n\n## Arguments\n\n- **`name`** *(optional)* — filters rows whose package path matches exactly\n  or as a `;`-prefixed group, or whose display name contains the string.\n\n## Examples\n\n```bash\nasd --list              # every package in the registry\nasd -l ndk               # anything matching \"ndk\"\nasd -l build-tools\n```\n\n```\nPACKAGE                                 NAME                                          VERSION\nndk;27.0.12077973                       NDK (Side by side)                            27.0.12077973\nndk;26.3.11579264                       NDK (Side by side)                            26.3.11579264\n```\n\nOutput is sorted and de-duplicated. If the registry doesn’t exist yet,\n`--list` doesn’t auto-download it — run [`--update`](/asd/commands/update/)\nfirst.",
      "Installed (--installed, -i)": "*Part of the [Command Reference](/asd/command-reference/).*\n\n```bash\nasd --installed [name]\nasd -i [name]\n```\n\nLists components actually present under `$ANDROID_HOME`, by scanning for\n`package.xml` files — independent of the registry, so it works even without\nrunning [`--update`](/asd/commands/update/) first.\n\n## Arguments\n\n- **`name`** *(optional)* — filters to package paths matching exactly or as\n  a `;`-prefixed group.\n\n## Examples\n\n```bash\nasd --installed          # everything installed\nasd -i ndk                 # just NDK entries\n```\n\n```\nPACKAGE                                 NAME                                          VERSION\nndk/27.0.12077973                       NDK (Side by side)                            27.0.12077973\nplatform-tools                          Android SDK Platform-Tools                    35.0.2\n```\n\nIf `$ANDROID_HOME` doesn’t exist, `asd` reports that instead of an empty\nlist:\n\n```\nSDK root not found at /home/you/Android/Sdk\n```",
      "Remove (--remove, -rm)": "*Part of the [Command Reference](/asd/command-reference/).*\n\n```bash\nasd --remove <name>\nasd -rm <name>\n```\n\nDeletes an installed component’s directory under `$ANDROID_HOME`.\n\n## Arguments\n\n- **`name`** *(required)* — the package path to remove, matched exactly or\n  as a `;`-prefixed group against entries in the registry.\n\n## Examples\n\n```bash\nasd --remove ndk\nasd -rm build-tools\n```\n\n## What it does\n\nLooks up every registry entry whose path matches `name`, then deletes the\ncorresponding directory under `$ANDROID_HOME` for each one that’s actually\npresent on disk. This means `asd --remove ndk` removes *every* installed NDK\nversion, not just one — remove a specific version with\n[`--fix`](/asd/commands/fix/)’s pattern in reverse if you only want one\nversion gone (delete the specific `$ANDROID_HOME/ndk/<version>` directory\nyourself).\n\nRemoving requires the registry (to resolve `name` to a path), so it will\ntrigger a registry download on first use just like\n[`install`](/asd/commands/install/).\n\n## Errors\n\n```yaml\nError: No packages matching 'foo' found in registry.\nError: No installed components found for 'ndk'.\n```",
      "Fix (--fix, -f)": "*Part of the [Command Reference](/asd/command-reference/).*\n\n```bash\nasd --fix [name]\nasd -f [name]\n```\n\nScans installed components for a broken/incomplete extraction — currently\ndetected as a `package.xml` with no other files alongside it — and\nreinstalls any that are affected.\n\n## Arguments\n\n- **`name`** *(optional)* — check only components whose `package.xml` path\n  contains this string. Omit it to check everything under `$ANDROID_HOME`.\n\n## Examples\n\n```bash\nasd --fix          # check and repair everything installed\nasd -f ndk           # check and repair just the NDK\n```\n\n```\nChecking all installed packages for issues ...\nFixing: NDK (Side by side) (27.0.12077973) ...\nFixed 1 package(s).\n```\n\n```\nNDK (Side by side) (27.0.12077973) is OK.\n```\n\n## What it does\n\nFor each candidate component, `asd` counts the files in its directory\n(excluding `package.xml`). If that count is zero, it deletes the directory\nand reinstalls the exact same version via the same path as\n[`install`](/asd/commands/install/) — including SHA-1 verification.\n\nSee [Repairing & Verifying](/asd/guides/repairing-verifying/) for a fuller\nwalkthrough, including how this differs from\n[`--verify`](/asd/commands/verify/).",
      "Update (--update, -up)": "*Part of the [Command Reference](/asd/command-reference/).*\n\n```bash\nasd --update\nasd -up\n```\n\nDownloads the latest package registry and saves it to `$ASD_CSV`.\n\n## What it does\n\n1. Downloads `sdk-archives.csv` from `https://maven.myket.ir/sdk-archives.csv`.\n2. Filters out rows tagged `windows`, `macos`, or `darwin`, keeping only\n   Linux-relevant entries.\n3. Writes the result to `$ASD_CSV` (`/opt/myket.csv` by default), using\n   `sudo` only if the target directory isn’t already writable.\n\n```\nFetching registry from https://maven.myket.ir/sdk-archives.csv ...\nRegistry updated. 4128 Linux packages available.\n```\n\n## When you need this\n\n- On first use, before installing anything (though\n  [`install`](/asd/commands/install/) will also auto-fetch the registry if\n  `$ASD_CSV` doesn’t exist yet).\n- Periodically, to pick up newly published SDK component versions.\n\nSee [Bypassing sudo](/asd/guides/bypassing-sudo/) if you’d rather point\n`$ASD_CSV` at a user-owned path so this never prompts for a password.",
      "Verify (--verify, -v)": "*Part of the [Command Reference](/asd/command-reference/).*\n\n```bash\nasd --verify <name> [version]\nasd -v <name> [version]\n```\n\nRe-downloads a component’s archive and compares its SHA-1 hash against the\nchecksum recorded in the registry, to confirm an installed component is\nauthentic and uncorrupted.\n\n## Arguments\n\n- **`name`** *(required)* — the package path to verify.\n- **`version`** *(optional)* — a specific version; defaults to the first\n  matching registry entry.\n\n## Examples\n\n```bash\nasd --verify ndk 27.0.12077973\nasd -v platform-tools\n```\n\n```\nDownloading archive to verify checksum ...\nRegistry checksum:  3a7f1e2c9b8d4f6a1e5c7b9d3f2a8e6c4b1d9f7a\nDownloaded checksum: 3a7f1e2c9b8d4f6a1e5c7b9d3f2a8e6c4b1d9f7a\nChecksum OK. Archive is authentic.\n```\n\nBecause this re-downloads the full archive to hash it, it’s a heavier\noperation than [`--fix`](/asd/commands/fix/) — use it when you specifically\nneed to confirm integrity, not as routine maintenance.\n\n## Errors\n\n```yaml\nError: <name> is not installed at <path>\nNo checksum available in registry for <name>.\nError: Checksum mismatch! Archive may be corrupted or tampered with.\n```"
    },
    "Project": {
      "Contributing": "Bug reports, feature requests, and pull requests are welcome on\n[GitHub](https://github.com/psdkjoon/asd).\n\n## Filing a bug\n\nPlease include:\n\n- The command you ran and its exact output.\n- Your distro and shell (`bash --version`).\n- Whether the affected directories (`$ANDROID_HOME`, `$ASD_CSV`) are\n  user-owned or required `sudo`.\n- The output of `asd --list <name>` if the issue is about a specific\n  package not resolving correctly.\n\n## Requesting a feature\n\nOpen an issue describing the workflow you’re trying to support — `asd` is\nintentionally a single Bash script with no dependencies beyond common\nCLI tools, so proposals that keep that footprint small are easiest to land.\n\n## Pull requests\n\n- Keep changes focused; separate unrelated fixes into separate PRs.\n- Match the existing style: `set -euo pipefail`, small single-purpose\n  functions (`do_install`, `do_list`, …), and `run_priv` for anything that\n  might need elevated permissions.\n- Test both the system-wide (`sudo ./setup`) and user-only\n  (`./setup ~/.local/bin ...`) install paths — see\n  [Bypassing sudo](/asd/guides/bypassing-sudo/).\n- Update `README.md` and these docs if you change command behavior or add a\n  new flag.\n\n## License\n\n`asd` is [MIT licensed](https://github.com/psdkjoon/asd/blob/main/LICENSE).",
      "FAQ": "### `asd` says the CSV file wasn’t found — what do I do?\n\nRun [`asd --update`](/asd/commands/update/) to download the registry, or set\n`ASD_CSV` to a location where it already exists. Any command that reads the\nregistry will show this if `$ASD_CSV` points somewhere empty.\n\n### Do I need `sudo` to use `asd`?\n\nOnly if the directories you’re using (`$ANDROID_HOME`, `$ASD_CSV`, or the\ninstall target for `setup`) aren’t writable by your user. Point them at\nuser-owned paths and `asd` never calls `sudo`. See\n[Bypassing sudo](/asd/guides/bypassing-sudo/).\n\n### `asd <name>` says the package wasn’t found. What’s wrong?\n\nEither the registry is stale (run [`--update`](/asd/commands/update/)) or\nthe name doesn’t match. Confirm the exact package path with\n[`asd --list <name>`](/asd/commands/list/) — package paths use `;` to\nseparate a group from its version, e.g. `ndk;27.0.12077973`.\n\n### How is this different from `sdkmanager`?\n\n`asd` fetches archives from the Myket mirror instead of Google’s servers,\nresolves package lookups from a local CSV instead of a live index, and\ndownloads with `aria2c` for multi-connection speed. It writes the same\n`package.xml` layout `sdkmanager` expects, so both tools — and Android\nStudio — recognize components installed by either one.\n\n### Why does `--verify` re-download the whole archive?\n\nThe SHA-1 checksum is only meaningful against the original archive, and\n`asd` doesn’t keep downloaded archives around after extraction — so\nverifying integrity means fetching it again. See\n[Repairing & Verifying](/asd/guides/repairing-verifying/) for when to reach\nfor `--verify` versus the lighter-weight [`--fix`](/asd/commands/fix/).\n\n### Can I use `asd` on Windows?\n\nNot directly — it’s a Bash script. The registry itself is also filtered to\nexclude Windows/macOS-only entries on every [`--update`](/asd/commands/update/).\nWSL works fine since it’s just Bash plus the usual CLI tools.\n\n### What if `--remove` deletes more than I expected?\n\n`asd --remove ndk` removes every installed version matching `ndk`, not a\nsingle one — see [`--remove`](/asd/commands/remove/) for the exact matching\nrules before running it."
    }
  }
}
